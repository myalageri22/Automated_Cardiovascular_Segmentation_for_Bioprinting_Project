"""Topology-aware postprocessing experiment for coronary CTA segmentation.

This package NEVER modifies authoritative artifacts. It reads frozen predictions
and writes exclusively under ``outputs/topology_correction/``.
"""
__version__ = "1.0.0"
